# 溯·赎

## 题意

给定两张存在障碍物的地图，每走一步自动切换到另一张地图，同时允许一次原地切换，判断能否从第一张图的 $(1,1)$ 出发，最终抵达第二张图的 $(n,m)$。

## Solution

设定四种状态 $(x,y,id,f)$ 做一次 $bfs$。

其中 $(x,y)$ 代表当前坐标，$id$ 代表处于哪张地图，$f$ 代表是否使用过法宝。

用 $vis_{x,y,id}$ 记录一下 第 $id$ 张图的 $(x,y)$​ 是否能从起点到达即可。
